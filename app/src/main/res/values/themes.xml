<resources xmlns:tools="http://schemas.android.com/tools">

    <!--Splash Screen API-->
    <style name="Theme.CustomSplashScreenTheme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/movie_screen</item>
        <item name="windowSplashScreenAnimationDuration">500</item>
        <item name="postSplashScreenTheme">@style/Theme.TheVillageRoadShow</item>
    </style>

    <!-- Base application theme. -->
    <style name="Base.Theme.TheVillageRoadShow" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/blue_grey_500</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="android:statusBarColor">@color/purple_700</item>
    </style>

    <style name="Theme.TheVillageRoadShow" parent="Base.Theme.TheVillageRoadShow" />

</resources>